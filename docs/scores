The scores will be stored in an SQLite database. This is the 'heavyweight'
solution, as scoreboards will require frequent searching and ammending.

Game highscores will be stored locally in
"~/.local/share/lw-games/tetris/scores", or optionally specified in the
configuration file.
